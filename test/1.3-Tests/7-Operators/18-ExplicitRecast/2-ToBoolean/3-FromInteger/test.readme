This test checks that the explicit recast operator correctly casts integer
values to boolean types.  Note that boolean values cannot be implicitly cast to
strings so they are first implicitly cast to integer values which are then
implicitly cast to strings.
