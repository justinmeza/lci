This test checks that the explicit cast operator correctly casts nil values to
boolean types.  Note that boolean values cannot be implicitly cast to strings
so they are first implicitly cast to integer values which are then implicitly
cast to strings.
