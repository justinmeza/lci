This test checks that the logical OR n-ary operator produces the correct truth
table.  Note that boolean values can not be cast to strings so this test first
implicitly casts the resulting boolean values to integer values and prints
these out instead.
