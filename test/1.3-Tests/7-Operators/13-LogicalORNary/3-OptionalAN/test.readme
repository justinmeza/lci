This test checks that the argument divider token (AN) for the logical OR n-ary
operator is optional.  Note that boolean values can not be cast to strings so
this test first implicitly casts the resulting boolean values to integer values
and prints these out instead.
