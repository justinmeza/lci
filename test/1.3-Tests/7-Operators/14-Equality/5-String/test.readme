This test checks that the equality operator works correctly with string typed
arguments.  Note that boolean values can not be cast to strings so this test
first implicitly casts the resulting boolean values to integer values and
prints these out instead.
