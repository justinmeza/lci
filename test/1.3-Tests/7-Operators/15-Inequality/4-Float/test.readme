This test checks that the inequality operator works correctly with floating
point decimal typed arguments.  Note that boolean values can not be cast to
strings so this test first implicitly casts the resulting boolean values to
integer values and prints these out instead.
